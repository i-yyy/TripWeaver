# Backend API
VITE_API_BASE_URL=http://localhost:8000

# AMap frontend
# Current frontend does not call weather / POI / static map HTTP URLs directly.
VITE_AMAP_WEB_KEY=
# Only needed if you later enable AMap JS map rendering in the browser.
VITE_AMAP_WEB_JS_KEY=your_amap_web_js_key_here
